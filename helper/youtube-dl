get() {
    _args='-c -f bestvideo[ext=mp4]+bestaudio[ext=m4a]/best[ext=mp4]/best'
    for _url in $(cat ~/in/get); do
        cd "~/in"
	youtube-dl $_args $_url 2&> ~/in/log & 
    done
}

get-add() {
    echo "$*" >> ~/in/get
}


